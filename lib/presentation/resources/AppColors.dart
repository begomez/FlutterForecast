import 'package:flutter/material.dart';

abstract class AppColors {
  static final Color primaryDark = Color(0xFF1D3557);
  static final Color primary = Color(0xFFA8DADC);
  static const Color accent = Color(0xFFE63946);

  static const Color actionColor = Colors.white;
  static const Color textColor = Colors.black;
  static const Color backgroundColor = Colors.white;
  static const Color trans = Colors.transparent;
}
